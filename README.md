# ethereum-simple-decentralized-application
A decentralized application (dapp) containing a smart contract deployed to an in-memory blockchain.

## Server 
`node_modules/.bin/ganache-cli`

## Client
`node index.js`

test
